@model System.Int32?
@{
    Dictionary<string, object> attributes = new Dictionary<string, object>();
    attributes["class"] = "form-control";
    attributes["maxlength"] = "10";
    }
@Html.TextBox("", (Model.HasValue) ? ViewData.TemplateInfo.FormattedModelValue : "", attributes)
